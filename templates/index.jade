extends ./layout/app.jade

block styles
    style.
        body {
            padding: 0;
            margin: 0;
        }

        .link {
            text-align: center;
            margin-top: 40px;
            font-size: 2em;
        }

block content
        a(href="/dashboard" class="link") To the dashboard ->
    

block scripts
    script(src="/socket.io/socket.io.js")
